<% provide(:title, "All Posts") %>
<h1>All Posts</h1>

<div class="row">
	<div class="col-xs-12">
		<%= will_paginate(inner_window: 1, outer_window: 1) %>

			<div class="posts">
				<%= render @posts %>
			</div>
	</div>
</div>