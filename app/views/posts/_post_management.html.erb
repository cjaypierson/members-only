<span class="post-management">
	<% if current_user && post.user.id == current_user.id %>
		<%= link_to "Edit", edit_post_path(post) %> | 
		<%= link_to "Delete", post, method: :delete, data: {confirm: 'Are you sure you wanto delete this post?' } %>
	<% end %>
</span>